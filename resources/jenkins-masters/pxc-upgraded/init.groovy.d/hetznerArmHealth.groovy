/**
 * pxc-upgraded clone fence stub. Same filename as production pxc's
 * hetznerArmHealth.groovy, so the boot sync overwrites the copy inherited inside the
 * restored JENKINS_HOME. The health probe operates on a Hetzner cloud the clone does not have.
 */
println("[hetznerArmHealth.groovy fence] no-op on the clone")

apiVersion: v2
name: jenkins-ingress
version: 0.1.0
description: NGINX reverse proxy behind the dedicated jenkins-masters ALB, serving the Jenkins master hostnames (PS-10945 SSL centralisation).
type: application

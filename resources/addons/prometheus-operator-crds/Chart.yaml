apiVersion: v2
name: prometheus-operator-crds
version: 0.1.0
description: |
  Standalone Prometheus-operator CRDs (ServiceMonitor, PodMonitor, Probe,
  PrometheusRule, etc.). Decoupled from kube-prometheus-stack so the
  CRDs survive when kps is removed (LGTM-only migration, ADR 0016).
type: application
dependencies:
  - name: prometheus-operator-crds
    version: 28.0.1
    repository: https://prometheus-community.github.io/helm-charts

apiVersion: v2
name: prometheus-node-exporter
version: 0.1.0
description: |
  Standalone prometheus-node-exporter — DaemonSet emitting per-node
  CPU/mem/disk/network metrics. Decoupled from kube-prometheus-stack
  so it survives kps removal (LGTM-only, ADR 0016).
type: application
dependencies:
  - name: prometheus-node-exporter
    version: 4.55.0
    repository: https://prometheus-community.github.io/helm-charts

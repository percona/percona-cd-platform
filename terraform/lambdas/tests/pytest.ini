[pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*

markers =
    aws: exercises AWS APIs (mocked with moto)
    iam: static assertions over the rendered IAM in the *.tf files

addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings
